wapower.localhost</code> con il tuo dominio reale</p>
                    <p><strong>3. Deploy:</strong> Il file è già configurato in docker-compose.yml per deployment automatico</p>
                    <p><strong>4. Logs:</strong> I log di accesso e errori sono in <code class="bg-yellow-100 px-1 rounded">/var/log/nginx/